spring:
    application:
        name: userservice
    
    datasource:
        url: jdbc:mysql://localhost:3306/   ?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
        username: root
        password: ${DB_PASSWORD}
    jpa:
        hibernate:
            ddl-auto: update
            dialect: org.hibernate.dialect.MySQL8Dialect
